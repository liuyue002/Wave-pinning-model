Python code for 2D finite elements simulation. Uses the FEniCS library (https://fenicsproject.org/).

I recommend using the Docker method for installation. Direct install can result in many dependency errors.
